<mah:BaseMetroDialog x:Class="Learn.MultipleFrameworks.Views.DialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:regions="http://prismlibrary.com/"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             MinWidth="300" MinHeight="200"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel
            HorizontalAlignment="Center" 
            VerticalAlignment="Center">
            <!-- <TextBlock  -->
            <!--     Text="Hello World!"  -->
            <!--     FontSize="28"  />  -->
            
            <ContentControl regions:RegionManager.RegionName="DialogContentRegion" />
        </StackPanel>
        
        <StackPanel
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom">
            <Button 
                Content="OK" 
                Padding="25 5" 
                Margin="15" 
                Command="{Binding PressCloseCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type mah:BaseMetroDialog}}}"/>
        </StackPanel>
    </Grid>
</mah:BaseMetroDialog>
