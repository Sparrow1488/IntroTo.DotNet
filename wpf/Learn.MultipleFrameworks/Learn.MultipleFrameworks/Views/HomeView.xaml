<UserControl x:Class="Learn.MultipleFrameworks.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding DialogClosureTime}" FontSize="16" />
            <TextBlock Text="{Binding FormInputValue}" FontSize="16" />
            <TextBlock Text="Hello Prism!" FontSize="28" />
            <WrapPanel HorizontalAlignment="Center">
                <Button Content="Read" Command="{Binding OpenDialogCommand}" Width="50" />            
                <Button Content="Input" Command="{Binding OpenInputDialogCommand}" Width="50" />            
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
